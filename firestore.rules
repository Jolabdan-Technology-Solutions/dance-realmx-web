rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WIDE OPEN FOR TESTING - NO SECURITY
    match /{document=**} {
      allow read, write: if true;
    }
  }
}